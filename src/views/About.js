import React from 'react';

export function About() {
    return <div>
        This is a *very* simple forum application that we're using as a demo / starting point for a coding exercise.
    </div>
}